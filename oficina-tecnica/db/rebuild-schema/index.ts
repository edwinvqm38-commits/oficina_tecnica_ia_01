/**
 * REBUILD-1B — Punto de entrada del schema Drizzle (BORRADOR LOCAL).
 *
 * Reexporta todas las definiciones de tablas y tipos desde `core.ts`.
 * No conecta a ninguna base de datos ni ejecuta migraciones.
 */

export * from "./core";
